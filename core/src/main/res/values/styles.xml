<resources>

    <!-- Colors -->
    <style name="LibraryOfAlexandria" parent="@style/Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:navigationBarColor">@color/navigationBar</item>
        <item name="android:statusBarColor">@color/backgroundDarkest</item>
        <item name="android:colorBackground">@color/colorBackground</item>
        <item name="android:windowAllowEnterTransitionOverlap">true</item>
        <item name="android:windowAllowReturnTransitionOverlap">true</item>
        <item name="android:windowSharedElementsUseOverlay">false</item>
    </style>

    <!-- Toolbar -->
    <style name="LibraryOfAlexandriaDarkOverlay" parent="@style/ThemeOverlay.MaterialComponents.Dark.ActionBar">
    </style>

    <style name="LibraryOfAlexandria.Translucent" parent="@style/LibraryOfAlexandria">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowBackground">@color/scrim</item>
    </style>


    <style name="Widget.LibraryOfAlexandria.Toolbar" parent="@style/Widget.MaterialComponents.Toolbar">
        <item name="android:background">?attr/colorPrimary</item>
        <item name="android:elevation">@dimen/one</item>
        <item name="android:theme">@style/LibraryOfAlexandriaDarkOverlay</item>
    </style>

    <style name="Widget.LibraryOfAlexandria.Toolbar.Dark">
        <item name="android:background">@color/backgroundDarkest</item>
        <item name="android:elevation">@dimen/zero</item>
        <item name="materialThemeOverlay">@style/ThemeOverlay.MaterialComponents.Dark.ActionBar</item>
        <item name="android:navigationBarColor">@color/navigationBar</item>
    </style>

    <!-- TextView -->
    <style name="TextAppearance.ItemMenu" parent="@style/TextAppearance.MaterialComponents.Body1">
        <item name="android:textColor">@color/textPrimaryLight</item>
        <item name="android:textSize">16sp</item>
    </style>


    <!-- Misc -->
    <style name="LibraryOfAlexandria.Launcher"/>

    <style name="LibraryOfAlexandria.Cards"/>

    <style name="LibraryOfAlexandria.About"/>

    <style name="Widget.LibraryOfAlexandria" parent="android:Widget"/>

</resources>
